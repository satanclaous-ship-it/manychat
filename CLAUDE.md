# CLAUDE.md — auto-dm (인스타그램 댓글 자동 DM)

이 레포는 허브(`satanclaous-ship-it/contents`)에서 분리된 **앱 코드 레포**다. 매니챗의 댓글→자동 DM 기능을 자체 구현한다.

## 이 레포에서 일할 때
- **설계 원본은 `docs/`다.** 01-plan(범위·정책 제약)·02-design(아키텍처·스키마·API·엔진)·03-build-guide(마일스톤). 코드가 설계와 어긋나면, 임의로 코드를 바꾸지 말고 먼저 `docs/`를 고친 뒤 구현한다.
- 빌드는 `docs/03-build-guide`의 마일스톤(M0~M5) 순서를 따른다. 각 마일스톤은 그 안의 판정 기준을 통과해야 다음으로 넘어간다.
- 변경 후에는 항상 `npm test` + `npx tsc --noEmit`. 웹훅/트리거/시퀀스 로직을 바꾸면 해당 픽스처 테스트를 먼저 갱신한다.
- 시크릿(APP_SECRET·토큰 등)은 코드·커밋에 절대 넣지 않는다 (02-design §8). 로컬은 `.dev.vars`(gitignore됨).

## 동기화
- 허브 레포가 모든 기획 문서의 유일한 원본이다. `docs/`는 허브 `outputs/2026-07-14-auto-dm-*`의 사본 — 기획을 고칠 땐 허브 원본도 함께 갱신한다.
- 작업 후 커밋 + 지정 브랜치 푸시.

## Meta 정책 (설계가 지키는 선 — 위반 금지)
- 24시간 메시징 윈도우: 상대 인바운드(DM·스토리답장) 후 24h 안에만 자유 발송. 댓글은 윈도우를 열지 않는다.
- Private reply: 댓글당 1회, 7일 이내. 이게 댓글→DM의 유일한 진입로.
- 개발 모드 단일 계정 전용. HUMAN_AGENT 태그(자동화 금지)·브로드캐스트는 쓰지 않는다.
